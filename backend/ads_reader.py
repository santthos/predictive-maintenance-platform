import random

def read_ads_data():
    # Mock function, replace with real pyads logic
    return {
        "sensor1": round(random.uniform(0, 100), 2),
        "sensor2": round(random.uniform(0, 50), 2),
        "timestamp": "2025-04-11T10:00:00Z"
    }